# Character Sheets

Back to [rules index](rules.md).

---

This link points to a Streamlit app rather than a static document. The fetched page redirects to Streamlit authentication, so no static app content was available to convert to Markdown.
